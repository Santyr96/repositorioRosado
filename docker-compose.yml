

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hairbooker_web
    ports:
      - "8080:80"        # Puerto para Apache
      - "3000:3000"      # Puerto para Vite (si usas este puerto)
    volumes:
      - .:/var/www/html   # Montar el código fuente
      - ./public/build:/var/www/html/public/build # Montar solo la carpeta build
    networks:
      - hairbooker_network
    depends_on:
      - db
    environment:
      APP_ENV: development
      APP_DEBUG: "true"
      DB_HOST: dpg-cu0joa8gph6c73follng-a
      DB_PORT: 5432
      DB_DATABASE: hairbooker
      DB_USERNAME: adminrosado
      DB_PASSWORD: XJH6wMpyJuIH2AcOTCmw8uzofN4oV5Rm
    


  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: phpmyadmin
    ports:
      - "8082:80"
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      PMA_USER: adminrosado
      PMA_PASSWORD: rosadoruiz
    networks:
      - hairbooker_network
    depends_on:
      - db

networks:
  hairbooker_network:
    driver: bridge

volumes:
  db_data:
